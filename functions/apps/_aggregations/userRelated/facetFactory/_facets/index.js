const dialogs = require('./dialogs/dialogs')
const doms = require('./doms/doms')
const events = require('./events/events')
const extracts = require('./extracts/extracts')

module.exports = { 
  dialogs,
  doms,
  events,
  extracts
}