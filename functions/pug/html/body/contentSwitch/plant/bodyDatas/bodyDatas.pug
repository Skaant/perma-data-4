.row
  h2.text-center.col-12.mt-4= `🛎🤩🌼 ${ translation.sections.data } 🌲`
.row
  .col-md-8.offset-md-2.col-12.container
    if (clouds)
      each cloud in clouds
        p.h4.my-4= translation.clouds[cloud.key]
        each data in cloud.datas
          include ./dataItem/dataItem
      